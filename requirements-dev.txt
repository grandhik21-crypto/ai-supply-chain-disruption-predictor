# Development and CI tooling.
#
# Install with:
#   python3 -m pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0.0,<9.0.0          # Test runner
ruff>=0.4.0,<1.0.0            # Fast linter and formatter
